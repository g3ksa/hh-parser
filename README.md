# hh-parser

# Запуск программы

-  С помощью docker:

   ```bash
   docker compose -f docker-compose.yml up -d
   ```

-  Без помощи docker:

   ```bash
   cd ./app
   npm start
   cd ../server
   python app.py
   ```
